# Сессия 2026-06-06 — «антисырость»: финансы, дашборд, PWA, полировка

## Что сделано (6 коммитов, всё на проде bilimos.kg)

| Блок | Что | Где |
|---|---|---|
| Деньги родителя | Таб «Оплата» в дневнике: счета, оплачено/остаток, пеня красным | `diary/page.tsx`, API `/fee-invoices/mine` (row-level RBAC + серверная пеня) |
| Приём платежей | Модал «Принять оплату», статус pending→partial→paid транзакцией | `workspace/accounting`, POST `/api/v1/payments` |
| Напоминания о долге | Кнопка у бухгалтера → событие `invoice.overdue` → алерт родителю (инбокс+TG+пуш) | `engine.ts` правило `invoice-overdue-parent`, POST `/fee-invoices/send-reminders` |
| Дашборд собственника | `/finance`: KPI, собираемость (ring), топ должников, динамика 6 мес | `finance/page.tsx`, GET `/api/v1/finance/summary` |
| PWA | manifest + иконки + `sw.js` (БЕЗ кэша — на `/` лендинг!) + регистрация | `app/manifest.ts`, `public/sw.js`, `PwaRegister` |
| Web-push | Модель `PushSubscription`, `/api/v1/push/subscribe`, канал в `notify.ts`, кнопка на `/agent` | no-op без VAPID env |
| Полировка | Toasts на все падения (журнал+ResourcePage), сортировка/поиск журнала, edit счёта (PUT `/fee-invoices/[id]`), печать бланка счёта, `@media print` | |
| Безопасность | Закрыта RBAC-дыра: общий GET `/fee-invoices` теперь staff-only (раньше родитель читал чужие счета) | +6 e2e-проверок |

## Верификация
- build зелёный, `rbac-leaks` 20/20, `access-matrix` 28/28
- Прод проверен: parent1 видит пеню 216 сом, RBAC 403, /finance отдаёт долг 98k / 9 должников, push configured:true
- Фикс `e2e/auth.setup.ts` под новый экран логина (getByLabel вместо плейсхолдеров)

## Деплой-заметки
- Репо два: `origin` = Sijjia/school-erudit, **`erudit` = Ulfacar/erudit — его слушает Coolify**, пушить в оба
- Прод-БД = Neon (доступна снаружи) → `DATABASE_URL=<из Coolify env> npx prisma db push`
- VAPID-ключи на Coolify поставлены через API (POST `/applications/{uuid}/envs`, без `is_build_time`)
- seed-demo идемпотентен, прогнан на проде (демо-долг parent1: просроченный −30 дн + partial)
- Coolify app uuid: `vqjsp8033jekwr4vnxgxrdyd`, деплой ~15-20 мин

## Решения дня
- **Видеоконференции**: НЕ отдельный продукт, НЕ свой WebRTC. Модуль «онлайн-урок» внутри BilimOS: ссылка Jitsi/Meet к уроку в расписании, посещаемость/оценки в журнале. Команда согласна (Гульдэн: «это и нужно было», Урмат: готовые сервисы, с нуля не делаем). Ждём от Гульдэн источник про закон «оффлайн→онлайн»
- **Мобилка**: PWA сейчас (готово) → Capacitor для сторов потом → React Native никогда
- **Монетизация (Асылбек: $400/мес мало)**: порядок — школы в системе → онлайн-оплата с комиссией → B2C (AI-репетитор) → маркетплейс/финтех. Модель 30% подписка / 70% услуги — ок как цель

## Что осталось (по приоритету)
1. **До 11.06**: прогнать демо-сценарий на проде для семинара Даткайым (+скриншоты новых фич в деку, пайплайн shot-deck готов)
2. КП Эмиру — статус за Аланом
3. **Онлайн-оплата с комиссией** (Мбанк/Элсом) — первое после семинара, фундамент (счета/платежи) готов
4. Видео-модуль «онлайн-урок» (~неделя) — после подтверждения спроса
5. WhatsApp-адаптер в notify.ts; Академия BilimOS; бухучёт/1С — дальше
